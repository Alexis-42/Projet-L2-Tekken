#include <SDL2/SDL.h>
#include "../include/personnages.h"

typedef struct {
    char * nom;
    char * sprite;
    SDL_Texture * texture;
    int taille_y;
    int taille_x;
} Personnage;

void initPerso(Joueur * j, SDL_Surface ){
	j->perso.texture
}
